# ----------------------------------------
# PROD VERSION PROPERTIES
# ----------------------------------------
spring:
  profiles: prod
  datasource:
    url: jdbc:mysql://dbserver:3306/production?verifyServerCertificate=false&useSSL=false
    username: production
    password: production
    driverClassName: com.mysql.jdbc.Driver
    initialization-mode: never
  jpa: 
    hibernate: 
      ddl-auto: none
  session:
    store-type: redis
  redis:
    host: redis
    port: 6379

